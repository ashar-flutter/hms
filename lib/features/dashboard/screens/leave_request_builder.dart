import 'package:flutter/material.dart';

class LeaveRequestBuilder extends StatelessWidget {
  const LeaveRequestBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
